name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v1

      - name: Take photo of github.com
        uses: ./action
        with:
          url: https://github.com
          width: 2560
          height: 1440
